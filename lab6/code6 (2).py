"""Wapp to compute gcd of two numbers. Only use comments for showing output"""
import math

def compute_gcd(a, b):
    return math.gcd(a, b)

gcd_result = compute_gcd(60, 48)
print(gcd_result)
# Output: 12
